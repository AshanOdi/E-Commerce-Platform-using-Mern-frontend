# Copy this file's variable NAME into Vercel's dashboard (Project Settings
# -> Environment Variables) for the deployed site. Vite bakes VITE_ vars into
# the build at BUILD time, not runtime — set this before triggering a deploy,
# and redeploy after changing it.

# The deployed backend's exact origin (Render service URL)
VITE_BACKEND_URL=
